import React from 'react';
import { Grid } from '@material-ui/core';

const Home: React.FC = () => {
	return (
		<Grid item xs={12} sm={12} lg={12}>
			Home Page
		</Grid>
	);
};

export default Home;
